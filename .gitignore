.DS_Store

# Internal documentation (do not push to GitHub)
CLAUDE.md
NetvisorCronWorkFlow.md
NetvisorAPI.md
NetvisorAPICron.md
joulukuu-timesheet.md

# IDE/Editor configuration
.markdownlint.json

# Environment files with real credentials (NEVER push to GitHub)
**/.env
.env
saas-app/.env
login-form/.env

# Keep .env.example files in git but with placeholder values only
# Real credentials should only be in .env files (which are ignored)
